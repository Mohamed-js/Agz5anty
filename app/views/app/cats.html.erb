<br><br>
<div class="container max-width3 meds">
    <% @categories.each do |category| %>

      <section class="center catt">
        <div class="service center flex-row">
          <%= link_to category_path(category.id), class:"flex-col center "  do %>
            <%= image_tag category.image, class:"category-img rounded"  %>
            <h3 class="center position-absolute category-name">
              <%= category.name.capitalize%>
            </h3>
          <% end %>
        </div>
      </section>

    <% end %>
</div>
<br><br>
<br><br>
